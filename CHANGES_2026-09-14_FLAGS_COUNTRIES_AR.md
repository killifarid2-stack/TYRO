# نظام الأعلام والدول — WAB-TKD
**التاريخ:** 2026-09-14

## ما كان موجوداً قبل هذا التعديل

- قائمة دول مكتوبة يدوياً في `src/lib/tkd-data.ts` فيها **109 اختصار فقط**. أي لاعب من دولة خارج هذه القائمة **لم يكن بالإمكان إدخاله أصلاً** من شاشة الإدارة.
- خريطة يدوية ثانية (IOC → ISO2) داخل `src/lib/flags.ts`.
- **3 ملفات أعلام فقط** في `src/assets/flags/` (gb, ma, tr)، والباقي كان يعتمد على حزمة `flag-icons`.
- البحث في `CountryPicker` كان يطابق **نص الاختصار فقط** — لا يعمل بالاسم العربي ولا الفرنسي ولا الإنجليزي.
- لا توجد أي طريقة لاختيار **شكل** العلم أو معاينته قبل أن يظهر على شاشة البث.

---

## 1. قاعدة بيانات الدول الكاملة — `src/lib/countries.ts` (جديد)

**254 دولة وإقليم**، كل واحدة منها لها صورة علم حقيقية مضمّنة داخل التطبيق (لا يوجد أي مدخل يؤدي إلى علم فارغ).

كل دولة تحتوي على:

| الحقل | المعنى |
|---|---|
| `iso2` | رمز ISO المكوّن من حرفين، وهو أيضاً اسم ملف الصورة |
| `code` | **الاختصار الرسمي المستعمل في التايكواندو (IOC)** — `MAR`, `KOR`, `GER`, `SUI`, `MAS`… وهو ما يظهر فعلياً على أوراق النتائج وشاشات التلفزيون |
| `aliases` | كل الاختصارات البديلة (ISO3 + القديمة) حتى لا تتعطل بطولاتك المحفوظة سابقاً |
| `en` / `ar` / `fr` | اسم الدولة بلغات التطبيق الثلاث |
| `region` | القارة (لتجميع القائمة) |
| `noc` | هل للدولة لجنة أولمبية وطنية معترف بها |

**ملاحظة مهمة:** الاختصار المعروض هو IOC وليس ISO alpha-3، لأنهما مختلفان في حوالي 60 دولة (`GER` مقابل `DEU`، `SUI` مقابل `CHE`، `MAS` مقابل `MYS`). ورغم ذلك، الاختصار الثاني يبقى يعمل في البحث وفي البيانات القديمة.

إنجلترا واسكتلندا وويلز وإيرلندا الشمالية أصبحت **مداخل منفصلة** (`ENG` / `SCO` / `WAL` / `NIR`) بأعلامها الحقيقية، بعدما كانت كلها تعرض علم بريطانيا.

### التوافق مع البيانات القديمة
تم التحقق آلياً: **كل** الاختصارات الـ200 التي كانت في الخريطة القديمة ما زالت تُفسَّر بشكل صحيح. التغيير الوحيد هو الدول الأربع أعلاه (وهو تحسين مقصود).

---

## 2. مجموعتا الأعلام — `src/assets/flags-standard/` و `src/assets/flags-hd/` (جديد)

| المجموعة | المحتوى | الاستعمال الأفضل |
|---|---|---|
| **Standard 144×108** | 254 علم، كلها بنفس المقاس بالضبط | القوائم، الجداول، لوائح الاختيار — لأن كل علم يشغل نفس المربع |
| **HD (النسبة الأصلية)** | 253 علم عالي الدقة، كل علم بنسبته الحقيقية | شاشة البث والجمهور، أنيميشن استدعاء اللاعبين والفائز |
| **Vector (SVG)** | حزمة `flag-icons` الموجودة أصلاً | بديل تلقائي، يكبر لأي مقاس بدون تشويش |

**تنبيه:** صور HD التي أرسلتها كانت بعرض 2560 بكسل (5.3 ميغابايت). صورة بهذا الحجم تستهلك حوالي 17 ميغابايت من الذاكرة عند فك ترميزها في المتصفح، وهذا سيء جداً لتطبيق بث مباشر. تم تصغيرها إلى عرض **512 بكسل** (أكثر من كافٍ لشاشة 1920×1080) وضغطها، فأصبح حجمها **1.2 ميغابايت** بدل 5.3 — بدون أي فرق مرئي على الشاشة.

العلم الوحيد الناقص من مجموعة HD هو فنزويلا (`VE`)، ويتم استبداله تلقائياً بنسخة Standard.

كل الأعلام **محلية بالكامل** — شاشة الجمهور في القاعة تعمل بدون إنترنت.

---

## 3. نظام شكل العلم — `src/lib/flag-style.ts` (جديد)

الخيارات المتاحة:

- **نوع الصورة:** Standard / HD / Vector
- **الشكل:** مستطيل · زوايا دائرية · دائري · مربع
- **إطار** (يجعل الأعلام البيضاء مثل اليابان وكوريا مقروءة على خلفية فاتحة)
- **ظل**

### سلسلة البدائل التلقائية
`النوع المختار ← النوع الآخر ← SVG ← شارة نصية بالاختصار`

بمعنى: حتى لو كانت الدولة غير معروفة تماماً أو تعذّر تحميل الصورة، **لن تظهر فجوة فارغة على الهواء أبداً** — يظهر `MAR` كنص واضح بدل المربع الفارغ.

### الافتراضيات
الإعدادات الافتراضية (`standard` + `rectangle` + بدون إطار + ظل) مضبوطة عمداً بحيث يكون شكل التطبيق بعد التحديث **مطابقاً تماماً** لما كان عليه قبله. لا يتحرك أي عنصر في أي أنيميشن موجود إلا إذا غيّرت أنت النمط بنفسك.

---

## 4. الإعداد عام ومتزامن مع شاشة الجمهور — `src/context/FlagSettingsContext.tsx` (جديد)

شكل العلم إعداد **واحد لكل التطبيق**: الإدارة، الجداول، شاشة المشغل، ونافذة البث/الجمهور المنفصلة.

يستعمل نفس آلية المزامنة الموجودة في `I18nProvider` عندك:
`localStorage` + حدث `storage` + `BroadcastChannel` + حدث داخلي.

هذا ضروري لأن شاشة الجمهور تعمل في نافذة Electron مستقلة: عندما يغيّر المشغل شكل العلم، **يتغيّر على الهواء فوراً بدون إعادة تحميل**. شاشة الجمهور تقرأ الإعداد فقط ولا تكتبه أبداً.

كل قيمة قادمة من التخزين أو من نافذة أخرى تمرّ عبر `sanitizeFlagStyle()` — قيمة تالفة أو معدّلة يدوياً لا يمكنها أن تصل إلى شاشة البث.

---

## 5. `FlagImage` — أُعيدت كتابته بالكامل

`src/components/FlagImage.tsx` هو **العارض الوحيد للأعلام في كل التطبيق** (حوالي 30 شاشة تستعمله).

- نفس الخصائص القديمة (`code`, `size`, `className`, `style`) تعمل كما هي — لم يتغيّر أي سطر في الشاشات الثلاثين.
- خصائص جديدة اختيارية: `set`, `shape`, `border`, `shadow`, `fit`, `lang` — لتجاوز الإعداد العام في مكان واحد فقط (مثلاً فرض HD داخل أنيميشن الفائز بينما تبقى القوائم على Standard).
- `code` يقبل الآن أي صيغة: `MAR` أو `MA` أو `Morocco` أو `Maroc` أو `المغرب`.

---

## 6. `CountryPicker` — أُعيد بناؤه

`src/components/CountryPicker.tsx`

- **بحث بالاختصار أو بالاسم**: `mar` · `MA` · `MYS` · `Morocco` · `Maroc` · `المغرب`
- يتجاهل التشكيل العربي ويوحّد أشكال الألف والياء والتاء المربوطة، فـ «أَلْمَغْرِب» تجد المغرب
- ترتيب النتائج بالأولوية: تطابق اختصار كامل ← اسم يبدأ بالنص ← اختصار يبدأ بالنص ← اسم يحتوي النص
- عند التصفح بدون كتابة: **تجميع حسب القارة** مع عدد الدول في كل قارة
- تنقّل بالأسهم ↑↓ و Enter و Escape
- العلم الحقيقي معروض داخل كل سطر
- **مبدّل شكل العلم أسفل القائمة مباشرة** مع معاينة حية — أي أن من يختار الدولة يقرر أيضاً كيف سيظهر علمها
- الأقاليم (غوام، ريونيون…) مؤشَّر عليها ويمكن إخفاؤها بخيار `nocOnly`

---

## 7. لوحة شكل العلم — `src/components/FlagStyleControls.tsx` (جديد)

مكوّن مستقل قابل لإعادة الاستعمال، موضوع في **ثلاثة أماكن**:

1. داخل قائمة `CountryPicker` (نسخة مضغوطة)
2. في **شاشة الإدارة**، مباشرة تحت حقلَي جنسية اللاعبين — حتى ترى قبل بداية المباراة كيف سيُرسم العلم الذي اخترته للتو
3. في صفحة الأعلام المستقلة

المعاينة تظهر العلم على **خلفية بيضاء وخلفية سوداء في نفس الوقت**، لأن العلم الأبيض قد يختفي على لوحة فاتحة والعلم الداكن قد يختفي على سواد شاشة البث.

---

## 8. صفحة الأعلام والدول — `/flags` (جديدة)

`src/pages/FlagsSettingsPage.tsx` — متاحة من القائمة العلوية (أيقونة العلم).

تحتوي على:

1. **متصفّح الدول الـ254** — شبكة بالأعلام، بحث، وخيار «اللجان الأولمبية فقط»، وتجميع حسب القارة
2. **لوحة اختيار الشكل** مع معاينة حية
3. **جدول «كيف سيظهر»** — يعرض **كل التركيبات**: 3 أنواع صور × 4 أشكال = 12 صورة للدولة المختارة، مع تأشير الأنواع التي يتم فيها الاستبدال التلقائي
4. **منطقة اختبار البث** على خلفية سوداء حقيقية، تحاكي الاستعمالات الفعلية:
   - زاويتا HONG (أحمر) و CHUNG (أزرق)
   - الشريحة الصغيرة في لوحة النتائج
   - العلم الكبير لأنيميشن الفائز (160 بكسل)

---

## 9. الشاشات التي تم ربطها

| الملف | ما تغيّر |
|---|---|
| `src/components/AdminPanel.tsx` | حقلا جنسية اللاعبين: من `<select>` بـ109 دولة ← إلى `CountryPicker` بالقائمة الكاملة. أُضيفت لوحة «شكل الأعلام» مع زر يفتح صفحة `/flags` |
| `src/components/TournamentManager.tsx` | حقل الدولة في إضافة اللاعبين: من قائمة منسدلة خاصة تطابق الاختصار فقط ← إلى `CountryPicker` |
| `src/components/TopNav.tsx` | رابط «الأعلام والدول» |
| `src/App.tsx` | `FlagSettingsProvider` فوق شجرة المسارات + المسار `/flags` |
| `src/lib/flags.ts` | أصبح طبقة توافق رقيقة فوق النظام الجديد — كل الدوال القديمة (`getFlagUrl`, `getIso2`, `getCountryFlag`, `getLocalFlagUrl`) تعمل كما هي، فلم تحتج الشاشات الثلاثون لأي تعديل |
| `src/lib/tkd-data.ts` | `TKD_COUNTRIES` لم تعد مكتوبة يدوياً — أصبحت مشتقّة من قاعدة البيانات الكاملة |

---

## 10. إصلاح خطأ كان يمنع البناء (خارج نطاق طلبك)

`src/pages/BroadcastDesignStudioPage.tsx` السطر 1603 كان ينقصه قوس إغلاق `}` في زر `COPY STYLE → SELECTION`.

**المشروع كما هو على GitHub لم يكن يُبنى إطلاقاً** (`npm run build` يفشل). `tsc` لم يكن يلتقط الخطأ لكن `esbuild` يرفضه. تم إصلاحه بإضافة القوس الناقص فقط، بدون أي تغيير آخر في الملف.

---

## 11. تحسين حجم الحزمة — `vite.config.ts`

الحدّ الافتراضي في Vite يدمج أي ملف أصغر من 4 كيلوبايت كـ base64 داخل ملف JavaScript. مع 507 علم صغير، كان 366 منها يُدمج داخل ملف الدخول الوحيد، فيرتفع حجمه إلى **4.1 ميغابايت** — وهو الملف الذي يجب على نافذة Electron تحليله بالكامل قبل ظهور أي شيء.

تمت إضافة استثناء لمجلدات الأعلام فقط، فأصبحت تُصدَّر كملفات مستقلة تُحمَّل عند الحاجة:

**4.1 MB ← 2.55 MB** لملف الدخول (انخفاض 38%)، والأعلام ما زالت محلية 100%.

---

## 12. الاختبارات

`src/test/countries-flags.test.ts` (جديد) — **14 اختبار، كلها ناجحة**:

- تفرّد اختصار كل دولة
- إيجاد الدولة من الاختصار / ISO2 / الاسم بالثلاث لغات
- تجاهل التشكيل العربي
- بقاء 21 اختصاراً قديماً/ISO3 يعمل (`VNM→VIE`, `DEU→GER`, `CHE→SUI`, `MYS→MAS`…)
- الدول الأربع البريطانية لها مداخل منفصلة
- عدم حدوث أي خطأ مع مدخل فارغ أو غير معروف
- ترتيب نتائج البحث
- خيار اللجان الأولمبية فقط
- التجميع حسب القارة بدون فقدان أي دولة
- رفض إعداد شكل تالف
- مطابقة الافتراضيات للشكل القديم

### حالة التحقق

```
npx vitest run src/test/countries-flags.test.ts   ✅  14/14
npm run build                                      ✅  ينجح
```

تم التحقق آلياً بمطابقة البصمات (md5) أن **507/507 صورة علم موجودة فعلاً داخل مجلد البناء** `dist/`.

---

## ⚠️ مشاكل موجودة مسبقاً في المستودع (لم ألمسها)

هذه **ليست** من تعديلاتي، وهي موجودة في `lommmm.git` قبل أن أبدأ. أذكرها حتى لا تفاجئك:

1. `src/components/BroadcastDesignStudioPreview.tsx` — 4 أخطاء أنواع (TS2322 / TS2741) عند تشغيل `tsc -p tsconfig.app.json`. لا تمنع البناء لأن `vite` يتجاهل فحص الأنواع، لكنها تعني أن بيانات الأنيميشن التجريبية في تلك الشاشة لا تطابق الأنواع الحقيقية.
2. اختبار واحد فاشل في مجموعة `broadcast-design` يخص `evaluateLayerAtTime` (الإطارات المفتاحية). المجموع: **76 ناجح / 1 فاشل**.

لم أعدّلهما لأنهما خارج نطاق ما طلبته، وإصلاح الثاني يحتاج قراراً منك حول السلوك الصحيح للإطارات المفتاحية. أخبرني إن أردت أن أتولاهما.

---

---

## 13. إصلاح كل الأخطاء المتبقية (طلب لاحق)

بعد التسليم الأول، طلبت إصلاح كل شيء متبقٍ. تم فحص المشروع بالكامل بأداتين: `npx tsc --noEmit` (على كل من `tsconfig.json` و `tsconfig.app.json`) و `npx vitest run`. النتيجة النهائية:

```
tsc --noEmit                 ✅  صفر أخطاء (كلا الإعدادين)
npx vitest run               ✅  77 / 77
npm run build                ✅  ينجح
```

### الأخطاء الستة التي تم إصلاحها

| # | الملف | المشكلة | الإصلاح |
|---|---|---|---|
| 1 | `src/components/PublicScoreboard.tsx` | دالة `TeamWinnerScreen` كانت تستعمل متغيراً اسمه `isPublicWindow` غير معرَّف في نطاقها (كان معرَّفاً فقط داخل دالة أخرى `ScoreboardView`) — يتحكم في إظهار زر «تعديل شاشة العرض» | أضفت نفس السطر المستعمل في `ScoreboardView`: `const isPublicWindow = isPublicDisplayWindow() || isMiniPreview;` داخل `TeamWinnerScreen` نفسها |
| 2 | `src/components/TournamentFileCenter.tsx` | دالة `WeightCard` تستعمل دالة مساعدة `T(en,ar,fr)` لعرض نص بثلاث لغات، لكن `T` كانت معرَّفة فقط في المكوّن الأب `TournamentFileCenter` (17 استعمال متأثر) | أضفت نسخة محلية من نفس الدالة داخل `WeightCard` (باستعمال `lang` من `useI18n`، لأن `t` هنا اسم خاص بسجل المباراة) |
| 3 | `src/lib/broadcast-design-advanced.ts` | متغير `right` في `evaluateCondition` كان نوعه محصوراً بـ `string` تلقائياً، فرفض TypeScript إسناد رقم أو boolean له لاحقاً (تحويل شرط مثل `score > 5` أو `active === true`) | حدّدت نوعه صراحة: `string \| number \| boolean \| null` |
| 4 | `src/lib/broadcast-design.ts` | `'videoSrc'` كانت موجودة فعلياً في قائمة الخصائص القابلة للأنيميشن (`ANIMATABLE_PROPERTIES`) ومُستعملة في كل مكان (استيراد فيديو، طبقة نوع video) لكنها كانت ناقصة من تعريف النوع نفسه `AnimatableLayerProperty` | أضفتها إلى النوع |
| 5 | `src/lib/design-studio-advanced.ts` | `evaluateFrameByFrame(design, fps=60)` كانت تمرر `fps` كـ `number` عام إلى دالة أخرى تتطلب بالضبط أحد معدلات الإطارات القياسية `24\|25\|30\|50\|60` | حدّدت نوع المعامل صراحة بنفس القائمة `24\|25\|30\|50\|60` |
| 6 | `src/lib/i18n.ts` | تحويل نوع مباشر لجدول الترجمات كان مرفوضاً لأن كل لغة تحتوي نصوصاً حرفية مختلفة تماماً (مثلاً `"Home"` بالإنجليزية مقابل `"الرئيسية"` بالعربية) فاعتبرته TypeScript تحويلاً غير منطقي | مررت بـ `unknown` أولاً كما اقترحت رسالة الخطأ نفسها: `as unknown as Partial<...>` |

كل الإصلاحات كانت **إما استعادة سطر كود ناقص وإما تصحيح نوع (type)** — لم يتغيّر أي سلوك أو منطق فعلي للمباراة أو البث. لا شيء من هذا متعلق بميزة الأعلام؛ كلها كانت أخطاء موجودة في المستودع الأصلي قبل أي تعديل مني.

## كيف تجرّب


```bash
npm install
npm run dev
```

1. افتح **الأعلام والدول** من القائمة العلوية (أو `/flags` مباشرة)
2. اكتب `mar` — يظهر المغرب أولاً. جرّب أيضاً `المغرب` و `Maroc` و `MA`
3. بدّل بين **Standard** و **HD** وبين الأشكال الأربعة، وراقب جدول «كيف سيظهر» ومنطقة اختبار البث
4. افتح شاشة **الجمهور** في نافذة ثانية، ثم غيّر الشكل من نافذة الإدارة — سيتغيّر على الشاشة الثانية فوراً
